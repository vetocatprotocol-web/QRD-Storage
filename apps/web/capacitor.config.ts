import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vetocatprotocol.qrdstorage',
  appName: 'QRD Storage',
  webDir: 'out',
};

export default config;
